import { writeFileSync } from 'node:fs';
import openapiTS, { astToString, UNKNOWN } from 'openapi-typescript';

const SPEC_URL = 'https://api.supabase.com/api/platform-json';
const OUTPUT_PATH = new URL('../src/platform-api/types.ts', import.meta.url);

function transform(schemaObject, options) {
  if (/JsonValue(?:_Output)?$/.test(options.path ?? '')) {
    return UNKNOWN;
  }
  return undefined;
}

const ast = await openapiTS(SPEC_URL, { transform });

const output = `/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

${astToString(ast)}`;

writeFileSync(OUTPUT_PATH, output);
